apiVersion: v1
kind: Service
metadata:
  name: vendor-service
  namespace: dev
spec:
  type: ClusterIP
  selector:
    app: vendor-service
  ports:
    - name: http
      port: 5004
      targetPort: 5004
---
apiVersion: v1
kind: Service
metadata:
  name: vendor-service
  namespace: prod
spec:
  type: ClusterIP
  selector:
    app: vendor-service
  ports:
    - name: http
      port: 5004
      targetPort: 5004


